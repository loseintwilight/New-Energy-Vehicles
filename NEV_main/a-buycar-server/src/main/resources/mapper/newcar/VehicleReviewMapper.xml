<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.newcar.mapper.VehicleReviewMapper">

    <resultMap id="VehicleReviewResult" type="com.ruoyi.newcar.domain.VehicleReview">
        <id     property="reviewId"     column="review_id"      />
        <result property="vehicleId"    column="vehicle_id"     />
        <result property="userId"       column="user_id"        />
        <result property="orderId"      column="order_id"       />
        <result property="rating"       column="rating"         />
        <result property="content"      column="content"        />
        <result property="imageUrl"     column="image_url"      />
        <result property="isAnonymous"  column="is_anonymous"   />
        <result property="status"       column="status"         />
        <result property="createTime"   column="create_time"    />
    </resultMap>

    <select id="selectByVehicleId" parameterType="Long" resultMap="VehicleReviewResult">
        select * from stad_vehicle_review where vehicle_id = #{vehicleId} and status = '0'
    </select>

</mapper>