package com.ruoyi.charging;

/**
 * 充电桩管理服务模块配置类
 * 注意：此模块作为 ruoyi-admin 的子模块运行，不需要独立启动
 */
// @SpringBootApplication  // 移除 - 不作为独立应用启动
// @MapperScan           // 移除 - 由主应用的 MyBatis 配置统一扫描
public class ChargingServeApplication {
    // 保留此类仅用于标识模块入口
    // 如需独立运行，请创建单独的启动类并排除此文件
}
