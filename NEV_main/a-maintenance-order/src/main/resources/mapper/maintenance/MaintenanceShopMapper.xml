<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.mapper.MaintenanceShopMapper">

    <resultMap id="ShopResult" type="com.ruoyi.business.domain.StadMaintenanceShop">
        <result property="shopId"       column="shop_id"       />
        <result property="shopName"     column="shop_name"     />
        <result property="merchantId"   column="merchant_id"   />
        <result property="province"     column="province"      />
        <result property="city"         column="city"          />
        <result property="district"     column="district"      />
        <result property="address"      column="address"       />
        <result property="longitude"    column="longitude"     />
        <result property="latitude"     column="latitude"      />
        <result property="contactName"  column="contact_name"  />
        <result property="contactPhone" column="contact_phone" />
        <result property="servicesInfo" column="services_info" />
        <result property="rating"       column="rating"        />
        <result property="description"  column="description"   />
        <result property="status"       column="status"        />
        <result property="createTime"   column="create_time"   />
        <result property="merchantName" column="merchant_name" />
    </resultMap>

    <sql id="selectShopVo">
        SELECT s.shop_id, s.shop_name, s.province, s.city, s.district,
               s.address, s.longitude, s.latitude, s.contact_name, s.contact_phone,
               s.services_info, s.rating, s.description, s.status, s.merchant_id,
               s.create_time, m.merchant_name
        FROM stad_maintenance_shop s
                 LEFT JOIN stad_merchant m ON s.merchant_id = m.merchant_id
    </sql>

    <select id="selectShopRanking" resultMap="ShopResult">
        <include refid="selectShopVo"/>
        ORDER BY s.rating DESC
    </select>

</mapper>