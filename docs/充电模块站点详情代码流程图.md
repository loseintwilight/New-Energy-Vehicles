```
站点详情页 (pages/mine/charge-pile/station-detail.vue)
    ├─ onLoad(options) 获取路由参数 stationId
    ├─  加载站点数据
    │   └─ GET /app/charge/station/{stationId}
    ├─  加载充电桩列表
    │   └─ GET /app/charge/pile/listByStation/{stationId}
    ▼
子页面跳转
    │
    ├─ 桩详情 → pile-detail.vue
    │   └─ GET /app/charge/pile/{pileId}
    │
    └─ 新增充电桩 → pile-add.vue
    │   └─ POST /app/charge/pile 保存新桩
    │用户点击"导航"按钮 → goNavi()
    │
    ├─ 校验站点坐标 (lat/lng)
    │   └─ 无坐标 → uni.showToast("暂无位置信息") → 终止
    │
    ├─ 获取用户当前位置
    │   └─ uni.getLocation({ type: 'gcj02', isHighAccuracy: true, timeout: 5000 })
    │
    ├─  调用高德驾车路径规划 API (多条路线)
    │   └─ amap.getDrivingRoutes({ fromLat, fromLng, toLat, toLng })
    │       └─ GET https://restapi.amap.com/v3/direction/driving
    ├─  路线选择切换
   		   └─ selectRoute(idx) → drawNaviRoute(idx)
             └─重新绘制 polyline + markers
```

## 小程序端站点详情 (用户侧)

```
小程序站点详情 (pages/station/detail.vue)
    │
    └─ GET /app/charge-mini/station/detail/{stationId}
        └─ StationController.detail()
            └─ StationServiceImpl.getStationDetail()
                │
                ├─ 查询站点: StationMapper.selectStationById()
                │   └─ SELECT * FROM stad_charging_station WHERE station_id = ?
                │       → 构建 StationDetailVO (name/address/lat/lng/totalPiles/parkFee/openTime/...)
                │
                ├─ 查询费率: StationMapper.selectRatesByStationId()
                │   └─ SELECT * FROM stad_charging_rate WHERE station_id = ?
                │       → 构建 RateVO 列表 (electricPrice/servicePrice/totalPrice/periodName)
                │
                ├─ 查询充电桩: PileMapper.selectPileListByStationId()
                │   └─ SELECT * FROM stad_charging_pile WHERE station_id = ?
                │       → 构建 PileVO 列表 (pileCode/pileType/powerKw/pileStatus/...)
                │
                └─ 实时统计: freePiles = piles.filter(p => p.pileStatus === '0').count
```

## 路线规划流程 (用户端站点详情 → 导航)

```

```

## 涉及文件

| 文件 | 作用 |
|------|------|
| `NEV-APP/pages/charge/detail.vue` | 站点详情页 - goNavi/drawNaviRoute/openExternalMap |
| `NEV-APP/utils/amap.js` | 高德地图工具 - navigateTo/getDrivingRoutes/getLocation |
| `NEV-APP/static/images/charge/car-marker.svg` | 路线起点标记图标 |
| `NEV-APP/static/images/charge/location-pin-blue.svg` | 路线终点标记图标 |
| `NEV-APP/static/images/charge/导航.png` | 导航入口图标 |

## 关键代码位置索引

| 功能 | 文件路径 |
|------|---------|
| 商户端站点详情页 | `NEV-APP/pages/mine/charge-pile/station-detail.vue` |
| 商户端站点API | `NEV-APP/api/charger/station.js` |
| 商户端充电桩API | `NEV-APP/api/charger/pile.js` |
| 小程序站点Controller | `NEV_main/.../chargeMini/StationController.java` |
| 小程序站桩Controller | `NEV_main/.../chargeMini/PileController.java` |
| StationService接口 | `NEV_main/a-chargeMini-serve/.../service/StationService.java` |
| StationServiceImpl | `NEV_main/a-chargeMini-serve/.../service/impl/StationServiceImpl.java` |
| StationDetailVO | `NEV_main/a-chargeMini-serve/.../vo/StationDetailVO.java` |
| StationMapper | `NEV_main/a-chargeMini-serve/.../mapper/StationMapper.java` |
| PileMapper | `NEV_main/a-chargeMini-serve/.../mapper/PileMapper.java` |
| 编辑站点页 | `NEV-APP/pages/mine/charge-pile/station-add.vue` |
| 充电桩列表页 | `NEV-APP/pages/mine/charge-pile/pile-list.vue` |
| 充电桩详情页 | `NEV-APP/pages/mine/charge-pile/pile-detail.vue` |
| 数据库建表 | `sql/new_energy.sql` |