# 充电页代码流程图（中间版）

---

```
打开充电页 (pages/charge/index.vue)
│
├─ 定位流程
│   ├─ 进入页面 → onLoad / onShow
│   │   └─ 调用 initLocation()
│   │       ├─ [一级] 尝试 uni.getLocation({ type: 'gcj02' })
│   │       │   ├─ 成功 → 更新 mapCenter.lat/lng
│   │       │   └─ 失败 → 进入二级降级
│   │       ├─ [二级] 调用高德SDK amap.getLocation()
│   │       │   ├─ 成功 → 更新 mapCenter
│   │       │   └─ 失败 → 进入三级降级
│   │       ├─ [三级] 使用默认坐标 (北京)
│   │       │   └─ 设置 mapCenter = 默认值
│   │       └─ 定位完成后 → 调用 fetchStationList()
│   │           └─ 携带 lat/lng 拉取附近电站
│   │
│   ├─ 手动选城市
│   │   └─ 点击城市选择器 → goCitySelect()
│   │       └─ navigateTo → /pages/charge/city-select.vue
│   │           ├─ onLoad → GET /app/charge/city/list
│   │           │   └─ CityController.list()
│   │           │       └─ stationMapper.selectCityList()
│   │           │           └─ 返回城市列表
│   │           └─ 用户点击某个城市
│   │               └─ 回传选中城市 + 对应坐标
│   │                   ├─ 更新 currentCity
│   │                   ├─ 更新 mapCenter 为选中城市坐标
│   │                   └─ 重新 fetchStationList()
│   │
│   └─ 逆地理编码获取城市名
│       └─ 定位成功后 → amap.reverseGeocode(lat, lng)
│           └─ 更新 currentCity（显示在顶部城市选择器）
│
├─ 地图展示附近站点标记 (mapMarkers)
│   ├─ fetchStationList()
│   │   └─ GET /app/charge-mini/station/list
│   │       ├─ 参数: lat, lng, pageNum, pageSize, filter, orderByColumn, isAsc
│   │       └─ StationController.list()
│   │           └─ StationServiceImpl.getStationList()
│   │               ├─ stationMapper.selectStationListWithDistance()
│   │               │   └─ SQL: 按经纬度计算距离 + 排序 + 分页
│   │               ├─ 循环 enrichStationItem()
│   │               │   ├─ 填充空闲桩数 / 总桩数
│   │               │   ├─ 填充标签 (快充/慢充/即插即充)
│   │               │   ├─ 填充最低价格
│   │               │   └─ 标记最近站点
│   │               └─ 返回 { rows: 站列表, total: 总数 }
│   │
│   ├─ 渲染地图 <map>
│   │   ├─ latitude / longitude / scale="15"
│   │   ├─ markers
│   │   │   └─ buildMapMarkers() → 每个站点生成一个标记
│   │   │       ├─ id, latitude, longitude
│   │   │       ├─ iconPath (自定义图标)
│   │   │       └─ callout (气泡标签: 站名 + 价格)
│   │   ├─ @markertap → onMarkerTap(e) → 跳转详情
│   │   └─ @regionchange → onMapMove() → 拖拽后重新查询
│   │
│   └─ 地图控件
│       ├─ 修改定位按钮 → openLocationPicker()
│       └─ 重新定位按钮 → reLocate() → 回到当前坐标
│
├─ 电站列表卡片 (station-list)
│   ├─ 列表数据来自 fetchStationList() 返回的 rows
│   ├─ 每个卡片展示:
│   │   ├─ 电站图片 + 最近标签
│   │   ├─ 电站名称 + 省¥标签
│   │   ├─ 标签行 (快充/慢充/超充/即插即充/自助)
│   │   ├─ 距离 (km) + 上次充电时间
│   │   ├─ 价格 (元/度 起) + 快慢充类型 + 空闲桩状态
│   │   └─ 停车费信息
│   ├─ @click → goDetail(station)
│   │   └─ navigateTo → /pages/charge/detail?stationId=xxx
│   ├─ @scrolltolower → loadMore() 上拉加载更多
│   └─ @refresherrefresh → onRefresh() 下拉刷新
│
├─ 点击站点标记 / 卡片
│       │
│       ▼
│   GET /app/charge-mini/station/detail/{stationId}
│   └─ StationController.detail(stationId)
│       └─ StationServiceImpl.getStationDetail(stationId)
│           ├─ stationMapper.selectStationById(stationId)
│           │   └─ 查询充电站基本信息
│           ├─ stationMapper.selectRatesByStationId(stationId)
│           │   └─ 查询该站所有费率时段
│           ├─ pileMapper.selectPileListByStationId(stationId)
│           │   └─ 查询该站所有充电桩 + 实时状态
│           └─ 组装 StationDetailVO 返回
│
│       │
│       ▼
│   显示站点详情 (pages/charge/detail.vue)
│   ├─ 站点头部
│   │   ├─ 站点类型标签 + 站点名称
│   │   ├─ 运营状态 + 营业时间
│   │   ├─ 运营商名称
│   │   ├─ 标签列表 (快充/慢充/自助/即插即充/停车免费)
│   │   ├─ 站点图片轮播
│   │   └─ 地址 + 距您距离 + 导航按钮
│   │
│   ├─ 充电价格卡片
│   │   ├─ 当前电价 (大号显示)
│   │   ├─ 电费单价 / 服务费单价 / 停车费
│   │   └─ 查看时段详情 → 弹窗显示全天费率分段
│   │
│   ├─ 充电桩列表 + 空闲状态
│   │   ├─ 筛选栏: 全部 / 快充(N) / 慢充(N)
│   │   ├─ 每个桩卡片:
│   │   │   ├─ 状态图标 (空闲/充电中/故障)
│   │   │   ├─ 桩编号 + 复制按钮
│   │   │   ├─ 桩名称 + 枪编号
│   │   │   ├─ 充电价格范围
│   │   │   ├─ 规格 (直流/交流 + 功率 + 电压 + 标准)
│   │   │   └─ 实时状态行
│   │   │       ├─ 空闲 → "空闲中 可扫码充电"
│   │   │       ├─ 充电中 → SOC% | 电流 | 电压 | 预计剩余
│   │   │       └─ 故障 → 故障描述
│   │   └─ 点击空闲桩 → 跳转充电页
│   │
│   ├─ 充电预估 (calculator)
│   │   ├─ 当前电量 slider (0~100%)
│   │   ├─ 目标电量 slider (0~100%)
│   │   └─ 预计花费 (前端实时计算)
│   │
│   └─ 用户评价 (预留区域)
│       └─ 纯前端占位
│
├─ 搜索充电站
│   │
│   ▼
│   打开搜索页 (pages/charge/search.vue)
│   ├─ onLoad
│   │   ├─ 加载智能推荐列表 (前端本地数据)
│   │   ├─ 加载搜索历史 (localStorage)
│   │   └─ GET /app/charge-mini/station/hot → 热门搜索
│   │
│   ├─ 用户输入关键词 → onInput 防抖 300ms
│   │   └─ GET /app/charge-mini/station/suggest?keyword=xxx
│   │       └─ 返回联想建议列表
│   │
│   └─ 确认搜索
│       └─ GET /app/charge-mini/station/search
│           ├─ 参数: keyword, lat, lng, pageNum, pageSize
│           ├─ StationController.search()
│           │   └─ StationServiceImpl.searchStations()
│           │       └─ stationMapper.searchStations()
│           │           └─ SQL: 名称/地址模糊匹配 + 距离排序
│           └─ 渲染搜索结果列表 (复用 station-card 样式)
│
└─ 选择空闲枪 → 扫码/输入枪号
        │
        ▼
    确认充电 → 调用开始充电
    ├─ 校验用户余额
    ├─ 弹窗显示: 桩编号 + 电价 + 预计金额
    └─ 点击确认 → POST /app/charge-mini/order/start
        ├─ 参数: { stationId, pileNo, energy, soc }
        │
        ▼
    POST /app/charge-mini/order/start
    └─ OrderController.start()
        └─ OrderServiceImpl.startCharge(userId, stationId, pileNo, energy, soc)
            ├─ 1. pileMapper.selectPileByPileNo(stationId, pileNo)
            │   └─ 校验桩是否存在 + 状态是否为 0 (空闲)
            │
            ├─ 2. stationMapper.selectRatesByStationId(stationId)
            │   └─ 匹配当前时段费率
            │       ├─ 遍历费率列表
            │       ├─ 匹配 startTime ~ endTime
            │       └─ 获取 electricPrice + servicePrice
            │       └─ 兜底: 0.35 + 0.55 = 0.90元/度
            │
            ├─ 3. 计算预估总价
            │   └─ electricFee + serviceFee = totalPrice
            │
            ├─ 4. orderMapper.insertOrder()
            │   └─ 订单号: "CO" + 时间戳
            │   └─ 状态: charging
            │
            └─ 5. pileMapper.updatePileStatus(pileId, "1")
                └─ 更新桩状态为充电中
                └─ stationMapper.syncStationPileCounts(stationId)
        │
        ▼
    充电中 (pages/charge/charging.vue)
    ├─ 加载动画 → 进入充电主界面
    │
    ├─ 电池状态卡片
    │   ├─ 站点名称 + 充电桩编号
    │   ├─ 电池图标 (动态填充高度)
    │   ├─ 实时 soc 百分比
    │   ├─ 已充电量 (度)
    │   ├─ 已充时长 (分:秒)
    │   ├─ 已充金额 (¥)
    │   ├─ 目标电量
    │   └─ 当前功率 (kW)
    │
    ├─ 进度条 (0% — 50% — 100%)
    │   └─ 动态填充 + 进度点
    │
    ├─ 实时监控面板
    │   ├─ 电池温度 (正常绿/偏高黄/过高红)
    │   ├─ 充电电流 (A)
    │   ├─ 充电电压 (V)
    │   └─ 实时功率 (kW)
    │
    ├─ 轮询更新
    │   └─ setInterval 1秒 → GET /app/charge-mini/order/status/{orderId}
    │       └─ OrderServiceImpl.getChargeStatus(orderNo)
    │           ├─ 查询订单当前数据
    │           ├─ 计算充电进度 (模拟: 5秒充满测试模式)
    │           ├─ 计算已充电量 / 已充金额
    │           └─ 返回: chargePercent, chargedEnergy, chargedAmount
    │
    ├─ 当前费用 (根据费率时段动态计算)
    │   └─ 前端根据 electricPrice + servicePrice 实时计算
    │       └─ totalAmount = chargedEnergy × (electricPrice + servicePrice)
    │
    ├─ 手动停止 / 充满自停
    │   ├─ 手动停止
    │   │   ├─ 弹出确认弹窗 (显示当前电量 + 金额)
    │   │   └─ POST /app/charge-mini/order/stop
    │   │       └─ OrderController.stop()
    │   │           └─ OrderServiceImpl.stopCharge(orderId)
    │   │               ├─ orderMapper.updateOrderForStop() 更新结算数据
    │   │               ├─ pileMapper.updatePileStatus(pileId, "0") 释放桩
    │   │               └─ stationMapper.syncStationPileCounts(stationId)
    │   │
    │   └─ 充满自停 (轮询检测充电进度到达100%)
    │       └─ OrderServiceImpl.getChargeStatus() 中自动完成
    │           ├─ orderMapper.updateOrderForStop()
    │           ├─ pileMapper.updatePileStatus(pileId, "0")
    │           └─ stationMapper.syncStationPileCounts(stationId)
    │
    │       │
    │       ▼
    │   订单结算
    │   └─ PUT /app/charge-mini/order/stop (后端自动完成)
    │       ├─ total_energy (最终充电度数)
    │       ├─ total_amount (最终金额 = 电费 + 服务费)
    │       ├─ end_time (结束时间)
    │       ├─ duration (充电时长, 秒)
    │       ├─ end_soc (结束SOC)
    │       ├─ rate_detail (JSON格式记录所用费率)
    │       │   └─ { electricPrice, servicePrice, pileType }
    │       └─ order_status = completed (订单完成)
    │
    │       │
    │       ▼
    │   支付 → 打开订单弹窗
    │   ├─ 展示: 充电度数 / 金额 / 时长 / 单价 / 桩号
    │   ├─ 立即支付 → POST /app/charge-mini/order/pay
    │   │   └─ 参数: { orderId, payMethod: "wechat"|"balance" }
    │   │       └─ 后端: 更新 pay_status = paid
    │   │
    │   └─ 稍后支付 → 关闭弹窗 (可在"我的订单"中支付)
    │
    │       │
    │       ▼
    │   碳积分发放 (carbon_earned)
    │   └─ 支付完成后后端自动触发
    │       ├─ 根据充电度数计算碳积分
    │       │   └─ carbonPoints = total_energy × 系数 (如 0.5积分/度)
    │       ├─ 插入碳积分流水表
    │       └─ 更新用户碳积分余额
    │
    └─ 返回首页 or 查看订单
```

---

## API 接口清单

| 功能 | 接口 | 方法 | 调用时机 |
|------|------|:----:|---------|
| 附近电站列表 | `/app/charge-mini/station/list` | GET | 定位完成/拖拽地图/筛选排序 |
| 电站详情 | `/app/charge-mini/station/detail/{id}` | GET | 点击地图标记/列表卡片 |
| 搜索电站 | `/app/charge-mini/station/search` | GET | 确认搜索时 |
| 搜索建议 | `/app/charge-mini/station/suggest` | GET | 输入关键词时(防抖) |
| 热门搜索 | `/app/charge-mini/station/hot` | GET | 打开搜索页 |
| 收藏/取消收藏 | `/app/charge-mini/station/favorite` | POST | 点击收藏按钮 |
| 收藏状态 | `/app/charge-mini/station/favorite/status` | GET | 打开详情页 |
| 充电桩列表 | `/app/charge-mini/pile/list` | GET | 打开详情页 |
| 开始充电 | `/app/charge-mini/order/start` | POST | 确认充电 |
| 充电状态 | `/app/charge-mini/order/status/{id}` | GET | 轮询(1秒) |
| 停止充电 | `/app/charge-mini/order/stop` | POST | 手动停止 |
| 支付订单 | `/app/charge-mini/order/pay` | POST | 支付 |
| 城市列表 | `/app/charge/city/list` | GET | 打开城市选择页 |

---

## 纯前端 vs 前后端交互

```
纯前端:
├─ 定位 API (uni.getLocation / amap.getLocation)
├─ 地图渲染 + 拖拽 (map 组件)
├─ 逆地理编码 (amap.reverseGeocode)
├─ 导航 (uni.openLocation)
├─ 搜索历史 (localStorage)
├─ 智能推荐 (本地数据)
├─ 充电预估计算 (前端 computed)
├─ 监控面板模拟数据 (前端模拟)
└─ 分享 / 剪贴板 (uni API)

前后端交互:
├─ 附近电站 / 搜索 / 建议 / 热门
├─ 电站详情 + 桩列表 + 费率
├─ 开始充电 → 轮询状态 → 停止 → 支付
├─ 收藏 / 取消收藏
├─ 城市列表
└─ 碳积分发放 (支付后自动触发)
```