# 充电页代码流程图（详细版）

> 本文基于用户提供的模板结构，细化每个步骤的前后端交互、数据流及页面逻辑

---

## 一、整体页面路由结构

```
充电模块页面路由
│
├─ /pages/charge/index.vue         ← 充电地图首页（定位 + 地图 + 站列表）
│
├─ /pages/charge/detail.vue        ← 站详情页（桩列表 + 费率 + 收藏 + 导航）
│
├─ /pages/charge/charging.vue      ← 充电中页面（实时进度 + 监控 + 停止）
│
├─ /pages/charge/search.vue        ← 搜索页（搜索框 + 智能推荐 + 历史 + 热门）
│
├─ /pages/charge/city-select.vue   ← 城市选择页（城市列表）
│
├─ /pages/charge/notice.vue        ← 消息通知页（充电服务公告）
│
└─ /pages/charge/recharge.vue      ← 充值页面（支付入口）
```

---

## 二、完整数据流总览

```
┌─────────────────────────────────────────────────────────────────────┐
│ 前端页面层                                                          │
│  ┌──────────┐    ┌──────────┐    ┌───────────┐    ┌───────────┐   │
│  │ index.vue│───→│detail.vue│───→│charging.vu│───→│ search.vue│   │
│  │ (地图首页)│    │ (站详情)  │    │ (充电中)   │    │ (搜索页)   │   │
│  └──────────┘    └──────────┘    └───────────┘    └───────────┘   │
└────────────────┬──────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│ API 接口层 (api/charge/station.js + rate.js + pile.js)             │
│ 封装 request 请求，统一前缀 /app/charge-mini                        │
└────────────────┬──────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 后端 Controller 层                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐   │
│  │ StationController│  │  PileController  │  │  OrderController  │   │
│  │ /station/*       │  │  /pile/*         │  │  /order/*         │   │
│  └────────┬────────┘  └────────┬────────┘  └────────┬─────────┘   │
│           │                    │                     │              │
│  ┌────────▼────────────────────▼─────────────────────▼──────────┐  │
│  │ CityController      →  GET /app/charge/city/list              │  │
│  └──────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 后端 Service 层                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │ StationService    │  │  PileService      │  │  OrderService    │ │
│  │ getStationList()  │  │  getPileList()    │  │  startCharge()   │ │
│  │ getStationDetail()│  │  resetAllPiles()  │  │  getChargeStatus │ │
│  │ searchStations()  │  │                   │  │  stopCharge()    │ │
│  │ getSuggestions()  │  │                   │  │  payOrder()      │ │
│  │ getHotSearches()  │  │                   │  │  cancelOrder()   │ │
│  │ toggleFavorite()  │  │                   │  │  getOrderList()  │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│ MyBatis Mapper 层                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │ StationMapper     │  │  PileMapper       │  │  OrderMapper     │ │
│  │ selectStationList │  │  selectPileList   │  │  insertOrder     │ │
│  │ selectStationById │  │  updatePileStatus │  │  updateOrder     │ │
│  │ selectRatesBy...  │  │  resetAllStuck... │  │  selectOrderByNo │ │
│  │ syncStationPile.. │  │                   │  │                  │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 数据库 MySQL                                                        │
│  ┌──────────────────────┐  ┌──────────────────────┐                │
│  │ stad_charging_station│  │  stad_charging_pile   │               │
│  │ (充电站主表)          │  │  (充电桩表)           │               │
│  ├──────────────────────┤  ├──────────────────────┤                │
│  │ station_id (PK)      │  │  pile_id (PK)        │               │
│  │ station_name         │  │  station_id (FK)     │               │
│  │ address              │  │  pile_no             │               │
│  │ latitude / longitude │  │  pile_type (dc/ac)   │               │
│  │ total_piles          │  │  status (0=空闲/1=充 │               │
│  │ free_piles           │  │   电中/2=故障/3=预留)│               │
│  │ ...                  │  │  power / voltage     │               │
│  └──────────────────────┘  └──────────────────────┘               │
│  ┌──────────────────────┐  ┌──────────────────────┐                │
│  │ stad_charging_rate   │  │  stad_charging_order  │               │
│  │ (费率表)             │  │  (充电订单表)          │               │
│  ├──────────────────────┤  ├──────────────────────┤                │
│  │ rate_id (PK)         │  │  order_id (PK)       │               │
│  │ station_id (FK)      │  │  order_no (唯一)      │               │
│  │ start_time/end_time  │  │  user_id             │               │
│  │ electric_price       │  │  station_id          │               │
│  │ service_price        │  │  pile_no             │               │
│  │ pile_type (dc/ac)    │  │  energy / amount     │               │
│  └──────────────────────┘  │  status              │               │
│                             └──────────────────────┘               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、功能模块详细流程图

### 模块 1：充电地图首页 (index.vue)

```
打开充电页 (pages/charge/index.vue)
│
├─ created / onShow
│   ├─ 获取设备信息 (statusBarHeight, safeBottom, capsuleRect)
│   │   └─ 纯前端 uni.getSystemInfoSync / uni.getMenuButtonBoundingClientRect
│   │
│   ├─ initLocation()  ← 定位策略
│   │   │
│   │   └─ 最后执行 fetchStationList() 拉取附近电站
│   │
│   ├─ fetchStationList()  ← 获取附近充电站列表
│   │   └─ GET /app/charge-mini/station/list
│   │       ├─ 参数: lat, lng, pageNum, pageSize, orderByColumn, isAsc, filter
│   │       ├─ 后端: StationServiceImpl.getStationList()
│   │       │   ├─ stationMapper.selectStationListWithDistance(lat, lng, offset, pageSize, ...)
│   │       └─ 前端处理
│   │           ├─ stationList = res.rows
│   │           ├─ buildMapMarkers() 构建地图标记点
│   │           │   └─ 每个标记: { id, latitude, longitude, iconPath, width, height, callout }
│   │           └─ 标记最近站点标记高亮
│   │
│   ├─ loadCurrentCity()  ← 定位成功后逆地理编码获取城市名
│   │   └─ 纯前端 amap.reverseGeocode(lat, lng) → currentCity
│   │
│   └─ 初始化筛选 + 排序状态
│       ├─ filterTabs: [全部, 快充, 慢充, 超充, 即插即充, 自助, 停车免费]
│       └─ sortOptions: [智能排序, 距离最近, 价格最低, 空闲最多, 好评优先]
│
├─ 渲染页面
│   ├─ 地图层 <map id="chargeMap">
│   │   ├─ :latitude / :longitude / :scale
│   │   ├─ :markers="mapMarkers"  ← 附近站点地图标记（这是微信小程序 map 组件的核心数据绑定）
│   │   ├─ @markertap="onMarkerTap"  → 点击标记 → goDetail(station) （地图标记点的点击事件）
│   │   ├─ @regionchange="onMapMove"  → 地图视野变化 → 重新查询fetchStationList()
│   │   └─ 地图控件
│   │       ├─ 修改定位按钮 → openLocationPicker() 显示地点选择
│   │       └─ 重新定位按钮 → reLocate() 回到当前位置
│   │
│   ├─ 筛选栏 <scroll-view class="filter-bar">
│   │   ├─ 点击筛选标签 → onFilterChange(item)
│   │   │   └─ activeFilter = item.key → fetchStationList() 重新请求
│   │   └─ 点击排序 → onSortChange(s)
│   │       └─ currentSort = s.key → fetchStationList() 重新请求
│   │
│   └─ 电站列表 <scroll-view class="station-list">
│       ├─ 每个 station-card 展示:
│       │   ├─ 左: 电站图片 (stationImg)
│       │   ├─ 中: 名称 + 标签(快充/慢充/即插即充) + 距离/km + 上次充电时间
│       │   ├─ 下: 价格(元/度 起) + 快慢充标签 + 空闲桩状态(3/6空闲)
│       │   └─ 底: 停车费信息
│       │   @click="goDetail(station)"
│       │   └─ uni.navigateTo → /pages/charge/detail?stationId=xxx
│       │
│       ├─ @scrolltolower="loadMore"  ← 上拉加载更多
│       │   └─ pageNum++ → fetchStationList() 追加数据
│       │
│       └─ @refresherrefresh="onRefresh"  ← 下拉刷新
│           └─ 重置 pageNum → fetchStationList() 刷新
│
└─ 用户交互
    ├─ 点击地图标记 → onMarkerTap(e) → goDetail(station)
    ├─ 拖拽地图 → onMapMove() → debounce → fetchStationList()
    ├─ 点击城市选择器 → goCitySelect() → navigateTo city-select.vue
    ├─ 点击搜索框 → goSearch() → navigateTo search.vue
    └─ 点击通知 → goNotice() → navigateTo notice.vue
```

---

### 模块 2：搜索充电站 (search.vue)

```
打开搜索页 (pages/charge/search.vue)
│
├─ onLoad
│   ├─ 获取智能推荐列表 (recommendList) → 纯前端本地数据
│   ├─ 获取搜索历史 (historyList) → localStorage 缓存
│   │   └─ uni.getStorageSync('charge_search_history')
│   └─ 获取热门搜索
│       └─ GET /app/charge-mini/station/hot
│           ├─ 后端: StationServiceImpl.getHotSearches()
│           └─ 返回热门关键词列表 [{name, count, bgColor, icon}]
│
├─ 用户输入关键词
│   ├─ onInput 防抖 300ms
│   │   └─ GET /app/charge-mini/station/suggest?keyword=xxx
│   │       ├─ 后端: StationServiceImpl.getSuggestions(keyword)
│   │       └─ 返回联想建议列表
│   │
│   └─ 点击搜索/按回车 → doSearch()
│       └─ GET /app/charge-mini/station/search
│           ├─ 参数: keyword, lat, lng, pageNum, pageSize
│           ├─ 后端: StationServiceImpl.searchStations(keyword, lat, lng, pageNum, pageSize)
│           │   ├─ stationMapper.searchStations(keyword, lat, lng, offset, pageSize)
│           │   ├─ 按名称/地址模糊匹配 + 距离排序
│           │   └─ 返回 StationVO 列表
│           ├─ 前端: 保存关键词到 historyList (localStorage)
│           └─ 渲染搜索结果列表 (同首页 station-card 样式)
│
├─ 其他交互
│   ├─ 点击智能推荐卡片 → selectRecommend(rec) → 搜索该推荐词
│   ├─ 点击搜索历史标签 → doSearchWithKeyword(h) → 快速搜索
│   ├─ 清空搜索历史 → clearHistory() → localStorage 清除
│   └─ 切换视图模式 → toggleViewMode() → 列表/地图切换展示
│
└─ 渲染搜索结果
    ├─ results-count: "找到 N 个充电站"
    └─ 结果列表 (可滚动加载更多)
```

---

### 模块 3：站点详情 (detail.vue)

```
打开站点详情 (pages/charge/detail.vue?stationId=xxx)
│
├─ onLoad(options)
│   └─ loadDetail() → GET /app/charge-mini/station/detail/{stationId}
│       ├─ 后端: StationServiceImpl.getStationDetail(stationId)
│       │   ├─ 1. stationMapper.selectStationById(stationId)
│       │   │   → 获取充电站基本信息
│       │   │   → StationDetailVO { name, address, lat, lng, totalPiles, images, ... }
│       │   │
│       │   ├─ 2. stationMapper.selectRatesByStationId(stationId)
│       │   │   → 获取费率时段列表 (峰谷平)
│       │   │   → List<RateVO> { rateId, startTime, endTime, electricPrice, servicePrice, pileType }
│       │   │   → 计算当前时段电价 currentPrice
│       │   │
│       │   ├─ 3. pileMapper.selectPileListByStationId(stationId)
│       │   │   → 获取充电桩列表及实时状态
│       │   │   → List<PileVO> { pileId, pileNo, pileType, status, power, voltage, ... }
│       │   │   → 统计空闲/充电中/故障桩数
│       │   │   → 统计快充/慢充数量 (fastCount / slowCount)
│       │   │
│       │   ├─ 4. 标签组装 (tagList)
│       │   │   → 根据桩类型/设施/服务生成标签
│       │   │
│       │   └─ 5. 返回 StationDetailVO 汇总
│       │
│       └─ 前端渲染
│           ├─ 站点头部
│           │   ├─ 站点类型标签 (国网/特来电/星星充电)
│           │   ├─ 站点名称 + 运营状态
│           │   ├─ 营业时间
│           │   ├─ 运营商名称
│           │   ├─ 标签列表 (快充/慢充/即插即充/自助/停车免费)
│           │   ├─ 站点图片轮播
│           │   └─ 地址 + 距您xxxkm + 导航按钮
│           │
│           ├─ 充电价格卡片
│           │   ├─ 当前电价 (大号突出显示)
│           │   ├─ 电费单价 / 服务费单价 / 停车费
│           │   └─ 查看时段详情 → showAllPrices() 弹窗显示全天费率
│           │
│           ├─ 充电桩列表 (pile-section)
│           │   ├─ 筛选栏: 全部 / 快充(N) / 慢充(N)
│           │   ├─ 每个充电桩卡片:
│           │   │   ├─ 状态图标 (空闲/充电中/故障)
│           │   │   ├─ 桩编号 + 复制按钮
│           │   │   ├─ 桩名称
│           │   │   ├─ 枪编号
│           │   │   ├─ 充电价格 (min~max元/度)
│           │   │   ├─ 规格标签 (直流/交流 + 功率 + 电压 + 标准)
│           │   │   └─ 实时状态行
│           │   │       ├─ 空闲中 → "空闲中 可扫码充电"
│           │   │       ├─ 充电中 → SOC% | 电流A | 电压V | 预计剩余
│           │   │       └─ 故障 → 故障文字
│           │   └─ @click 选择空闲桩 → 跳转充电页
│           │       └─ uni.navigateTo → /pages/charge/charging?stationId=xxx&pileNo=xxx
│           │
│           ├─ 充电预估 (calculator-section)
│           │   ├─ 当前电量 slider (0~100%，步进5)
│           │   ├─ 目标电量 slider (0~100%，步进5)
│           │   ├─ 预计充电: 度数 + 金额 (前端实时计算)
│           │   └─ 计费说明文字
│           │
│           └─ 底部操作栏
│               ├─ 收藏按钮 → toggleFavorite(stationId)
│               │   ├─ POST /app/charge-mini/station/favorite
│               │   └─ 判断返回: 已收藏/取消收藏
│               ├─ 分享按钮 → shareStation()
│               │   └─ 纯前端 uni.share()
│               └─ 导航按钮 → goNavi()
│                   └─ 纯前端 uni.openLocation({ latitude, longitude, name, address })
│
├─ 其他交互
│   ├─ 查看时段详情 → showAllPrices()
│   │   └─ 弹窗展示全天所有费率时段 (00:00-08:00 低谷 ¥0.35 ...)
│   ├─ 复制桩编号 → copyPileCode(code)
│   │   └─ uni.setClipboardData
│   └─ 查看更多桩 → onViewMore() 展开/收起完整桩列表
```

---

### 模块 4：开始充电 (charging.vue)

```
进入充电页面 (pages/charge/charging.vue?stationId=xxx&pileNo=xxx&...)
│
├─ onLoad(options)  ← 从 detail.vue 跳转过来 (也可从首页快速扫码)
│   ├─ 获取参数: stationId, pileNo, stationName
│   ├─ 校验用户余额是否充足 → GET /app/charge-mini/order/balance
│   │   └─ 余额不足 → 弹窗引导充值
│   └─ 弹窗让用户确认开始充电
│       ├─ 显示: 充电桩编号 + 当前电价 + 预计金额
│       └─ 点击确认 → initCharge()
│           └─ POST /app/charge-mini/order/start
│               ├─ 参数: { stationId, pileNo, energy, soc }
│               ├─ 后端: OrderServiceImpl.startCharge(userId, stationId, pileNo, energy, soc)
│               │   ├─ 1. pileMapper.selectPileByPileNo(stationId, pileNo)
│               │   │   → 校验桩是否存在、状态是否为 0(空闲)
│               │   │   → 若不可用 → 返回错误 "充电桩不可用"
│               │   │
│               │   ├─ 2. stationMapper.selectRatesByStationId(stationId)
│               │   │   → 遍历费率时段, 匹配当前时间
│               │   │   → 根据桩类型(dc/ac)获取对应电价+服务费
│               │   │   → 兜底: 电费0.35 + 服务费0.55 = 0.90元/度
│               │   │
│               │   ├─ 3. 计算预估总价
│               │   │   → electricFee = energy * electricPrice
│               │   │   → serviceFee = energy * servicePrice
│               │   │   → totalPrice = electricFee + serviceFee
│               │   │
│               │   ├─ 4. orderMapper.insertOrder(...)
│               │   │   → 生成订单号: "CO" + 时间戳
│               │   │   → 插入充电订单表, status='charging'
│               │   │
│               │   └─ 5. pileMapper.updatePileStatus(pileId, "1")
│               │       → 更新桩状态为 1=充电中
│               │       → stationMapper.syncStationPileCounts(stationId) 同步站空闲数
│               │
│               └─ 后端返回: { orderNo, electricPrice, servicePrice, totalPrice }
│
├─ 充电中状态 → initPolling()  ← 轮询模式
│   ├─ 页面渲染 (pageLoading = false)
│   │   ├─ 电池动画卡片
│   │   │   ├─ 站点名称 + 充电桩编号
│   │   │   ├─ 电池图标 (动态填充高度 chargePercent%)
│   │   │   ├─ 百分比数字
│   │   │   └─ 充电数据区
│   │   │       ├─ 当前功率 (实时)
│   │   │       ├─ 已充电量 (度)
│   │   │       ├─ 已充时长 (分:秒)
│   │   │       ├─ 已充金额 (¥)
│   │   │       └─ 目标电量 (度)
│   │   │
│   │   ├─ 进度条
│   │   │   ├─ 0% — 50% — 100%
│   │   │   └─ 进度点 + 填充条
│   │   │
│   │   ├─ 充电信息行
│   │   │   ├─ 充电模式: 标准模式 (纯前端)
│   │   │   └─ 预计结束时间 (计算得出)
│   │   │
│   │   ├─ 实时监控面板
│   │   │   ├─ 电池温度 → 颜色状态 (正常绿/偏高黄/过高红)
│   │   │   ├─ 充电电流 (A)
│   │   │   ├─ 充电电压 (V)
│   │   │   └─ 实时功率 (kW)
│   │   │
│   │   └─ 功率变化趋势图 (模拟数据)
│   │       └─ 趋势点 + 曲线 → powerHistory 数组
│   │
│   ├─ startPolling()  ← 定时器
│   │   └─ setInterval 1秒 轮询
│   │       └─ GET /app/charge-mini/order/status/{orderId}
│   │           ├─ 后端: OrderServiceImpl.getChargeStatus(orderNo)
│   │           │   ├─ orderMapper.selectOrderByOrderNo(orderNo)
│   │           │   ├─ 计算充电时长 durationSecs
│   │           │   ├─ 模拟进度 (测试: 5秒充满)
│   │           │   │   → chargePercent = min(99, durationSecs * 100 / 5)
│   │           │   ├─ 计算已充电量 chargedEnergy
│   │           │   ├─ 计算已充金额 chargedAmount
│   │           │   ├─ 若已到目标时间 → 自动完成
│   │           │   │   ├─ orderMapper.updateOrderForStop(...)
│   │           │   │   ├─ pileMapper.updatePileStatus(pileId, "0") 释放桩
│   │           │   │   └─ stationMapper.syncStationPileCounts(stationId)
│   │           │   └─ 返回: { chargePercent, chargedEnergy, chargedAmount, status, ... }
│   │           │
│   │           └─ 前端更新数据
│   │               ├─ chargePercent / chargedEnergy / chargedAmount / chargedTime
│   │               ├─ 模拟: temperature / current / voltage / realTimePower
│   │               └─ 更新 powerHistory 功率趋势
│   │
│   └─ 充电完成 或 用户手动停止
│       ├─ 充电完成 → setInterval 停止 → 弹出订单详情弹窗
│       │   ├─ 展示: 充电度数 + 金额 + 时长 + 单价
│       │   └─ 按钮: [去支付] / [稍后支付]
│       │
│       └─ 手动停止 → confirmStop()
│           ├─ uni-popup 确认弹窗
│           │   ├─ 图标 + "确认停止充电？"
│           │   └─ "已充电 X 度，费用 ¥X.XX"
│           ├─ 点击 [继续充电] → cancelStop() 关闭弹窗
│           └─ 点击 [确认停止] → doStop()
│               └─ POST /app/charge-mini/order/stop
│                   ├─ body: { orderId }
│                   ├─ 后端: OrderServiceImpl.stopCharge(orderId)
│                   │   ├─ orderMapper.updateOrderForStop(...) 更新订单
│                   │   ├─ pileMapper.updatePileStatus(pileId, "0") 释放桩
│                   │   └─ stationMapper.syncStationPileCounts(stationId)
│                   ├─ 后端返回: 最终订单数据 (电量/金额/时长)
│                   └─ 前端: isStopped=true → 渲染已停止横幅 + 查看订单
│
├─ 支付流程
│   ├─ isStopped = true 后显示 "充电已停止" 横幅
│   ├─ 打开订单弹窗 → openOrderPopup()
│   │   ├─ 订单详情: 电量/金额/时长/单价/桩号/开始时间
│   │   └─ 按钮: [立即支付] → doPay() / [稍后支付] → 关闭弹窗
│   │
│   └─ doPay() → 选择支付方式
│       └─ POST /app/charge-mini/order/pay
│           ├─ body: { orderId, payMethod: "wechat"|"balance" }
│           └─ 返回支付结果 → 成功跳转订单列表
│
└─ 底部操作栏
    ├─ 未停止: 预估费用 + [停止充电] 按钮
    └─ 已停止: [查看订单] 按钮
```

---

## 四、API 接口详细清单

### 4.1 充电站接口 (StationController - `/app/charge-mini/station`)

| 序号 | 接口 | 方法 | 参数 | 返回说明 | 调用方 |
|:----:|------|:----:|------|---------|--------|
| 1.1 | `/list` | GET | lat, lng, pageNum, pageSize, orderByColumn, isAsc, filter | 分页电站列表(含距离/价格/空闲数) | index.vue |
| 1.2 | `/detail/{stationId}` | GET | stationId | 站详情(含桩列表+费率+标签+图片) | detail.vue |
| 1.3 | `/search` | GET | keyword, lat, lng, pageNum, pageSize | 模糊搜索结果列表 | search.vue |
| 1.4 | `/suggest` | GET | keyword | 输入联想建议列表 | search.vue |
| 1.5 | `/hot` | GET | 无 | 热门搜索关键词 | search.vue |
| 1.6 | `/favorite` | POST | stationId | 收藏/取消收藏(切换) | detail.vue |
| 1.7 | `/favorite/status` | GET | stationId | 查询是否已收藏 | detail.vue |

### 4.2 充电桩接口 (PileController - `/app/charge-mini/pile`)

| 序号 | 接口 | 方法 | 参数 | 返回说明 | 调用方 |
|:----:|------|:----:|------|---------|--------|
| 2.1 | `/list` | GET | stationId | 桩列表(含状态/功率/规格) | detail.vue |
| 2.2 | `/reset-all` | POST | 无 | 重置异常占用桩(管理用) | 管理工具 |

### 4.3 充电订单接口 (OrderController - `/app/charge-mini/order`)

| 序号 | 接口 | 方法 | 参数 | 返回说明 | 调用方 |
|:----:|------|:----:|------|---------|--------|
| 3.1 | `/list` | GET | pageNum, pageSize, status | 用户充电订单列表 | 订单列表页 |
| 3.2 | `/start` | POST | stationId, pileNo, energy, soc | 开始充电 → 返回订单号 | charging.vue |
| 3.3 | `/status/{orderId}` | GET | orderId | 充电实时状态(进度/电量/金额) | charging.vue |
| 3.4 | `/stop` | POST | orderId | 停止充电 → 释放桩 | charging.vue |
| 3.5 | `/pay` | POST | orderId, payMethod | 支付订单 | charging.vue |
| 3.6 | `/cancel` | POST | orderId | 取消未支付订单 | 订单列表页 |

### 4.4 城市接口 (CityController - `/app/charge/city`)

| 序号 | 接口 | 方法 | 参数 | 返回说明 | 调用方 |
|:----:|------|:----:|------|---------|--------|
| 4.1 | `/list` | GET | 无 | 城市列表 | city-select.vue |

---

## 五、前端-后端交互总览

```
纯前端实现（无后端交互）
├─ uni.getLocation / amap.getLocation 定位 (仅调用平台API)
├─ amap.reverseGeocode 逆地理编码 (仅调用地图SDK)
├─ map 组件渲染 + 拖拽地图 (地图SDK)
├─ markertap / regionchange 地图交互 (地图SDK)
├─ 导航 openLocation (地图SDK)
├─ 分享 uni.share (平台API)
├─ 剪贴板 uni.setClipboardData (平台API)
├─ 搜索历史 localStorage (本地存储)
├─ 充电模式/车况模拟数据 (纯前端)
├─ 功率趋势图模拟 (纯前端)
├─ 充电预估计算 (前端 computed)
├─ 筛选/排序前端过滤 (computed)
└─ 智能推荐列表 (硬编码本地数据)

有后端交互（前后端分离）
├─ 附近充电站列表          GET  /app/charge-mini/station/list
├─ 充电站详情              GET  /app/charge-mini/station/detail/{stationId}
├─ 搜索充电站              GET  /app/charge-mini/station/search
├─ 搜索建议                GET  /app/charge-mini/station/suggest
├─ 热门搜索                GET  /app/charge-mini/station/hot
├─ 收藏/取消收藏           POST /app/charge-mini/station/favorite
├─ 收藏状态                GET  /app/charge-mini/station/favorite/status
├─ 充电桩列表              GET  /app/charge-mini/pile/list
├─ 开始充电                POST /app/charge-mini/order/start
├─ 充电状态                GET  /app/charge-mini/order/status/{orderId}
├─ 停止充电                POST /app/charge-mini/order/stop
├─ 支付订单                POST /app/charge-mini/order/pay
├─ 取消订单                POST /app/charge-mini/order/cancel
├─ 订单列表                GET  /app/charge-mini/order/list
└─ 城市列表                GET  /app/charge/city/list
```

---

## 六、完整用户旅程

```
用户打开充电页
│
├─ 首页定位 + 地图展示
│   ├─ 自动定位到当前位置
│   ├─ 地图标记附近电站
│   └─ 下方列表展示电站卡片
│
├─ 用户交互
│   ├─ 拖拽地图 → 重新查询附近站
│   ├─ 点击筛选/排序 → 重新请求列表
│   ├─ 搜索 → search.vue → 搜索 / 联想 / 历史 / 热门
│   └─ 选择城市 → city-select.vue
│
├─ 选择电站 → 进入详情页
│   ├─ 查看站信息 + 费率 + 桩列表
│   ├─ 查看时段详情
│   ├─ 充电预估 (滑块模拟)
│   ├─ 收藏/分享/导航
│   └─ 查看充电桩状态 → 选择一个空闲桩
│
├─ 开始充电 → charging.vue
│   ├─ 确认充电对话框
│   ├─ POST /order/start → 创建订单 + 锁定桩
│   ├─ 轮询 /order/status → 实时更新进度
│   ├─ 实时监控面板 (温度/电流/电压/功率)
│   └─ 手动停止 / 自动完成
│
├─ 充电完成
│   ├─ 释放充电桩
│   ├─ 展示订单弹窗
│   └─ 立即支付 / 稍后支付
│
└─ 支付完成 → 结束旅程
    └─ 历史订单可在"我的"页面查看
```