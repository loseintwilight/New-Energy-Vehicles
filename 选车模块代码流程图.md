# 选车模块代码流程图

---

## 整体页面结构

```
选车模块首页 /pages/car/index.vue
│
├─ Banner 轮播图（3张静态图片）
│
├─ Tab 切换栏
│   ├─ 全新现车 (activeTab=0)
│   ├─ 二手车 (activeTab=1)
│   └─ 以旧换新 (activeTab=2)
│
├─ 筛选栏（仅 Tab 0 / Tab 1 显示）
│   ├─ 全部（重置筛选）
│   ├─ 颜色（picker 选择器）
│   ├─ 车型（picker 选择器）
│   └─ 价格（↑ 升序 / ↓ 降序 切换）
│
└─ 内容区（根据 activeTab 渲染不同组件）
    ├─ <NewCarContent />   → 全新现车列表
    ├─ <UsedCarContent />  → 二手车列表
    └─ <TradeInContent />  → 以旧换新表单
```

---

## 一、数据流总览

```
┌──────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  前端页面     │────▶│  API 接口层       │────▶│  后端 Controller │
│  .vue 文件    │◀────│  api/car/car.js  │◀────│  CarController  │
└──────────────┘     └──────────────────┘     └─────────────────┘
```

### 全部 API 接口清单（后端路径前缀 `/car`）

| 接口路径 | 方法 | 说明 |
|---------|------|------|
| `/car/list` | GET | 新车列表（分页） |
| `/car/detail` | GET | 新车详情 |
| `/car/used/list` | GET | 二手车列表（分页） |
| `/car/used/detail` | GET | 二手车详情 |
| `/car/used/inspection` | GET | 二手车检测报告 |
| `/car/order/create` | POST | 创建新车订购单 |
| `/car/tradein/evaluate` | POST | AI估价（固定返回3.50万） |
| `/car/tradein/order/create` | POST | 创建以旧换新订单 |
| `/car/finance/plans` | GET | 金融方案列表 |
| `/car/vehicle/specs` | GET | 车辆参数规格 |
| `/car/vehicle/reviews` | GET | 车辆评价列表 |
| `/car/config/colors` | GET | 颜色筛选选项 |
| `/car/config/models` | GET | 车型筛选选项 |
| `/car/config/cities` | GET | 城市列表 |
| `/car/config/stores` | GET | 门店列表 |

---

## 二、页面路由结构

```
选车模块页面路由
│
├─ /pages/car/index.vue            ← 选车首页（Tab容器 + 筛选）
│
├─ 独立入口页（从其他入口跳转用）
│  ├─ /pages/car/new-car.vue       ← 全新现车（仅 NewCarContent）
│  ├─ /pages/car/used-car.vue      ← 二手车（仅 UsedCarContent）
│  └─ /pages/car/trade-in.vue      ← 以旧换新（仅 TradeInContent）
│
├─ 订购流程页（navigateTo 进入）
│  ├─ /pages/car/order-new.vue     ← 新车订购（选配置 + 提交订单）
│  ├─ /pages/car/order-used.vue    ← 二手车订购（车况确认 + 提交订单）
│  └─ /pages/car/order-trade.vue   ← 以旧换新（三步流程：旧车→估价→换新确认）
│
└─ 组件（components/）
   ├─ NewCarContent.vue            ← 新车列表 + 筛选 + 订购跳转
   ├─ UsedCarContent.vue           ← 二手车列表 + 筛选 + 订购跳转
   └─ TradeInContent.vue           ← 以旧换新表单 + AI估价弹窗
```

---

## 三、功能模块详解

### Tab 0：全新现车

```
┌─────────────────────────────────────────────────────┐
│  index.vue                                           │
│  ┌─ created ──────────────────────────────────────┐  │
│  │  getFilterColors() → GET /car/config/colors     │  │
│  │  getFilterModels() → GET /car/config/models     │  │
│  └────────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ 筛选栏 ─────────────────────────────────────┐  │
│  │  selectedColor | selectedModel | priceSortDir  │  │
│  │   → computedFilter → 传给 NewCarContent        │  │
│  └────────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ NewCarContent.vue ──────────────────────────┐  │
│  │  created → fetchCarList()                     │  │
│  │    → GET /car/list → 返回 rows 数据           │  │
│  │                                               │  │
│  │  computed: filteredList                       │  │
│  │    1. filter.color 筛选                       │  │
│  │    2. filter.model 筛选                       │  │
│  │    3. sortDir 升序/降序排序                    │  │
│  │                                               │  │
│  │  ┌─ 卡片点击 ────────────────────────────┐   │  │
│  │  │  goDetail(car)                        │   │  │
│  │  │    → GET /car/detail?id=xxx           │   │  │
│  │  │    → GET /car/vehicle/specs?id=xxx    │   │  │
│  │  │    → GET /car/vehicle/reviews?id=xxx  │   │  │
│  │  │    → showModal 展示完整详情            │   │  │
│  │  │                                       │   │  │
│  │  │  goOrder(car) → $emit('go-order', m) │   │  │
│  │  │    → index.vue navigateTo             │   │  │
│  │  │      /pages/car/order-new             │   │  │
│  │  └───────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**执行流程：**

```
用户进入选车首页
  → created: 加载筛选选项（颜色、车型）
  → NewCarContent created: 加载新车列表
  → 渲染卡片列表
  → 用户点击筛选条件 → computed filteredList 前端过滤 + 排序
  → 用户点击卡片 → 三个API顺序调用 → showModal 展示详情
  → 用户点击「立即订购」→ 跳转 order-new.vue
```

---

### Tab 1：二手车

```
┌─────────────────────────────────────────────────────┐
│  UsedCarContent.vue                                  │
│  created → fetchCarList()                           │
│    → GET /car/used/list → 返回 rows 数据            │
│                                                     │
│  computed: filteredList（同全新现车，前端过滤排序）   │
│                                                     │
│  ┌─ 卡片点击 ──────────────────────────────────┐   │
│  │  goDetail(car)                              │   │
│  │    → GET /car/used/detail?id=xxx            │   │
│  │    → GET /car/vehicle/specs?id=xxx          │   │
│  │    → GET /car/used/inspection?id=xxx        │   │
│  │    → showModal 展示完整详情（含检测报告）      │   │
│  │                                             │   │
│  │  goOrder(car) → $emit('go-order', m)       │   │
│  │    → index.vue navigateTo                   │   │
│  │      /pages/car/order-used                  │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**区别说明：** 二手车比新车多调一个 `/car/used/inspection` 接口获取检测报告，卡片多展示"上牌日期/里程/过户次数"信息。

---

### Tab 2：以旧换新

```
┌─────────────────────────────────────────────────────┐
│  TradeInContent.vue（纯表单页面，无列表卡片）         │
│                                                     │
│  页面内容从上到下：                                   │
│  1. 顶部Hero图 + 置换流程说明（4步骤）                │
│  2. 旧车信息表单（品牌/车型/年份/里程/车况）           │
│  3. 联系人信息（姓名/电话/门店）                      │
│     → created: getStores() GET /car/config/stores   │
│  4. 心仪新车选择（picker 选择器）                     │
│     → created: fetchNewCars() GET /car/list         │
│  5. 置换权益展示（纯前端UI）                          │
│  6. 提交申请按钮                                     │
│                                                     │
│  ┌─ 提交流程 ──────────────────────────────────┐   │
│  │  submitTradeIn()                            │   │
│  │    → showEvalPopup = true（弹出AI估价弹窗）    │   │
│  │    → evalLoading = true                      │   │
│  │    → GET /car/tradein/evaluate（固定返回3.5） │   │
│  │    → evalLoading = false, 展示估价结果        │   │
│  │    → 展示置换价格计算（新车价 - 旧车价 - 补贴） │   │
│  │    → 用户点击「确认提交」                      │   │
│  │      → POST /car/tradein/order/create        │   │
│  │      → 返回成功 → navigateBack               │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**AI估价弹窗内容：**

```
┌─────────────────────────────┐
│  AI 智能估价                 │
│                             │
│  市场行情价: ¥X.XX          │
│  车况调整: +¥X.XX           │
│  里程调整: -¥X.XX           │
│  ─────────────────          │
│  AI最终估价: ¥X.XX          │
│                             │
│  置换价格计算:               │
│  新车 xxx: ¥X.XX            │
│  旧车抵扣: -¥X.XX           │
│  置换补贴: -¥8,000.00       │
│  ─────────────────          │
│  应付金额: ¥X.XX            │
│                             │
│  [重新填写]  [确认提交]      │
└─────────────────────────────┘
```

---

### 订购流程

#### 3.1 新车订购页 (order-new.vue)

```
用户从 NewCarContent 点击「立即订购」
  → navigateTo /pages/car/order-new?car=JSON
  → onLoad 解析 car 数据
  → fetchFinancePlans() GET /car/finance/plans?vehicleId=xxx
  → loadFavoriteStatus() 获取收藏状态

页面配置选项（均为前端本地数据）：
  ├─ 外观颜色（3种：海湾蓝/星环灰/珍珠白）
  ├─ 轮毂选配（3种：19寸/20寸/21寸）
  ├─ 交付方式（2种：门店自提/送车上门）
  ├─ 金融方案（从后端API获取）
  ├─ 联系人信息（表单填写）
  └─ 保险方案（3种：基础/全面/尊享）

底部操作栏：
  ├─ 预估总计（前端 computed 计算）
  ├─ 收藏按钮（前后端交互）
  │   ├─ 未收藏 → addCollection({ targetType:'vehicle', targetId })
  │   └─ 已收藏 → cancelCollectionByTarget('vehicle', targetId)
  └─ 提交订单按钮
      → submitOrder()
        → 校验联系人/手机号
        → showModal 确认
        → POST /car/order/create → 成功navigateBack
```

#### 3.2 二手车订购页 (order-used.vue)

```
用户从 UsedCarContent 点击「立即订购」
  → navigateTo /pages/car/order-used?car=JSON
  → 展示车辆详情（车况参数、配置信息、检测评级）
  → 提交订购 → POST /car/order/create（orderType = 'purchase'）
```

#### 3.3 以旧换新订购页 (order-trade.vue)

```
三步流程：
┌─ Step 1：旧车信息 ─────────────────────────────┐
│  品牌/车型/上牌年份/里程/车牌所在地/车况/上传照片  │
│  → 点击「开始估价」                              │
└────────────────────────────────────────────────┘
                      │
                      ▼
┌─ Step 2：在线估价 ─────────────────────────────┐
│  POST /car/tradein/evaluate                    │
│  → 展示估价结果（目前固定返回3.50万）            │
│  → 选择心仪新车（GET /car/list 获取列表）       │
│  → 展示置换价格计算                             │
│  → 点击「下一步」                                │
└────────────────────────────────────────────────┘
                      │
                      ▼
┌─ Step 3：换新确认 ─────────────────────────────┐
│  联系人信息 + 门店选择（GET /car/config/stores）│
│  → 点击「确认提交」                             │
│    → POST /car/tradein/order/create            │
│    → 成功navigateBack                          │
└────────────────────────────────────────────────┘
```

---

## 四、前后端交互汇总

| 功能点 | 接口路径 | 方式 | 所属模块 |
|--------|---------|------|---------|
| **筛选颜色选项** | `GET /car/config/colors` | 有前后端交互 | 首页筛选栏 |
| **筛选车型选项** | `GET /car/config/models` | 有前后端交互 | 首页筛选栏 |
| **新车列表** | `GET /car/list` | 有前后端交互 | 全新现车Tab |
| **新车详情** | `GET /car/detail` | 有前后端交互 | 新车卡片点击 |
| **车辆参数规格** | `GET /car/vehicle/specs` | 有前后端交互 | 新车/二手车详情 |
| **车辆评价** | `GET /car/vehicle/reviews` | 有前后端交互 | 新车详情 |
| **二手车列表** | `GET /car/used/list` | 有前后端交互 | 二手车Tab |
| **二手车详情** | `GET /car/used/detail` | 有前后端交互 | 二手车卡片点击 |
| **二手车检测报告** | `GET /car/used/inspection` | 有前后端交互 | 二手车详情 |
| **金融方案** | `GET /car/finance/plans` | 有前后端交互 | 新车订购页 |
| **门店列表** | `GET /car/config/stores` | 有前后端交互 | 以旧换新表单 |
| **城市列表** | `GET /car/config/cities` | 有前后端交互 | 以旧换新订购 |
| **AI估价** | `POST /car/tradein/evaluate` | 有前后端交互 | 以旧换新估价弹窗 |
| **新车订购** | `POST /car/order/create` | 有前后端交互 | 新车/二手车订购页 |
| **以旧换新订购** | `POST /car/tradein/order/create` | 有前后端交互 | 以旧换新提交 |
| **收藏操作** | `POST /app/collection/add` | 有前后端交互 | 新车订购页收藏 |
| **查询收藏状态** | `GET /app/collection/status` | 有前后端交互 | 新车订购页收藏 |
| **取消收藏** | `DELETE /app/collection/target` | 有前后端交互 | 新车订购页收藏 |
| Banner轮播图 | 无接口（本地静态图片） | 纯前端 | 首页 |
| 外观/轮毂/交付/保险选项 | 无接口（本地数据） | 纯前端 | 新车订购页 |
| 前端列表筛选排序 | 无接口（computed过滤） | 纯前端 | 全新现车/二手车 |

---

## 五、完整执行流程（从入口到下单）

```
用户进入选车模块
│
├─ /pages/car/index.vue
│   ├─ 渲染 Banner + Tab + 筛选栏
│   ├─ 加载筛选选项（颜色/车型）
│   └─ 根据 Tab 渲染对应组件
│
├─ Tab 0：全新现车
│   ├─ GET /car/list → 渲染车辆卡片
│   ├─ 前端筛选排序
│   ├─ 点击卡片 → 多API串联 → 弹窗展示详情
│   └─ 点击「立即订购」
│       └─ /pages/car/order-new.vue
│           ├─ GET /car/finance/plans → 金融方案
│           ├─ 用户选择配置 + 填写信息
│           ├─ 收藏/取消收藏
│           └─ POST /car/order/create → 下单成功
│
├─ Tab 1：二手车
│   ├─ GET /car/used/list → 渲染车辆卡片
│   ├─ 前端筛选排序
│   ├─ 点击卡片 → 多API串联 → 弹窗展示详情（含检测报告）
│   └─ 点击「立即订购」
│       └─ /pages/car/order-used.vue
│           ├─ 展示车况详情
│           └─ POST /car/order/create → 下单成功
│
└─ Tab 2：以旧换新
    ├─ 填写旧车信息表单
    ├─ GET /car/list → 选择心仪新车
    ├─ GET /car/config/stores → 选择门店
    ├─ POST /car/tradein/evaluate → AI估价弹窗
    └─ 确认提交
        └─ POST /car/tradein/order/create → 申请成功
```