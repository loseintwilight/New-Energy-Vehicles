# 首页 & 登录业务逻辑流程图

---

## 一、登录业务逻辑

### 整体流程

```
用户打开应用
│
├─ App.vue onLaunch
│   ├─ initConfig() → 加载全局配置
│   └─ checkLogin()
│       └─ 是否有 Token？
│           ├─ 有 → 留在当前页
│           └─ 无 → 重定向到 /pages/login
│
├─ 进入登录页 /pages/login.vue
│   ├─ created → getCode() 获取图形验证码
│   │   └─ GET /captchaImage → 返回验证码图片 + uuid
│   │
│   └─ 用户填写账号密码验证码
│       └─ 点击「登录」→ handleLogin()
│           └─ pwdLogin()
│               └─ this.$store.dispatch('Login', loginForm)
│                   │
│                   ├─ (1) Login Action → POST /login
│                   │   ├─ 参数: username, password, code, uuid
│                   │   └─ 返回: { token }
│                   │       ├─ setToken(token) → 存入 storage
│                   │       └─ commit SET_TOKEN → 存入 Vuex
│                   │
│                   └─ (2) 成功后 → loginSuccess()
│                       └─ this.$store.dispatch('GetInfo')
│                           └─ GET /getInfo
│                               └─ 返回: { user, roles, permissions }
│                                   ├─ SET_ID (userId)
│                                   ├─ SET_NAME (userName)
│                                   ├─ SET_AVATAR (avatar)
│                                   ├─ SET_PHONENUMBER (手机号)
│                                   ├─ SET_ROLES / SET_PERMISSIONS
│                                   └─ SET_CURRENT_END
│                                       └─ 根据用户名自动匹配端类型
│                                           ├─ matainc → merchant
│                                           ├─ dealer_a → business
│                                           ├─ charger_b → charging
│                                           └─ 其他 → user
│
│                   └─ (3) 全部完成后 → reLaunch('/pages/index')
│
└─ 进入首页
```

### 登录模块文件结构

| 文件 | 职责 |
|------|------|
| `pages/login.vue` | 登录页面 UI + 表单校验 + 调用登录 |
| `pages/register.vue` | 注册页面 |
| `api/login.js` | 封装登录/注册/获取信息/退出/验证码接口 |
| `store/modules/user.js` | Vuex 状态管理：Login、GetInfo、LogOut 三个 Action |
| `permission.js` | 页面跳转拦截器（未登录强制跳转登录页） |
| `App.vue` | 应用启动时检查登录状态 |

### API 接口清单

| 接口 | 方法 | 说明 |
|------|------|------|
| `/captchaImage` | GET | 获取图形验证码 |
| `/login` | POST | 用户登录，返回 token |
| `/getInfo` | GET | 获取当前用户详细信息 |
| `/register` | POST | 用户注册 |
| `/logout` | POST | 退出登录 |

### 登录状态拦截机制 (`permission.js`)

```
用户触发页面跳转（navigateTo/redirectTo/reLaunch/switchTab）
  → 拦截器检查 Token
  ├─ 有 Token → 放行（若跳转登录页则强制回首页）
  └─ 无 Token → 检查白名单
      ├─ 在白名单（login/register/webview）→ 放行
      └─ 不在白名单 → 强制跳转登录页
```

---

## 二、首页业务逻辑

### 整体页面结构

```
┌─────────────────────────────────────┐
│           Banner 轮播图              │  ← 有前后端交互
├─────────────────────────────────────┤
│  ┌─────────────┐  ┌──────────────┐  │
│  │ 用户头像/名称 │  │ 碳积分: xxx  │  │  ← 有前后端交互
│  │ 手机号       │  │              │  │
│  └─────────────┘  └──────────────┘  │
├─────────────────────────────────────┤
│  去充电    购车    碳积分   预约维保  │  ← 纯前端（页面跳转）
├─────────────────────────────────────┤
│  精选科普 │ 购车推荐 │ 政策解读      │  ← 有前后端交互
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ 卡片列表（科普/购车/政策）   │    │  ← 有前后端交互
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

---

### 功能模块详解

#### 1. Banner 轮播图

| 项目 | 说明 |
|------|------|
| **实现方式** | 有前后端交互 |
| **数据来源** | 本地静态图片数组 `newlist`（3张图） |
| **交互逻辑** | swiper 组件自动轮播，`interval: 5000` |
| **代码位置** | `pages/index.vue` → `<swiper>` 区域 |

```
用户打开首页 → 读取 newlist 图片数组 → swiper 自动轮播
```

---

#### 2. 用户信息卡片

| 项目 | 说明 |
|------|------|
| **用户名/手机号** | 从 Vuex Store 读取（登录时通过 `/getInfo` 接口获取） → **有前后端交互** |
| **碳积分余额** | 通过 API `/app/carbon/overview` 获取 → **有前后端交互** |
| **点击跳转** | 点击卡片跳转至「我的」页面 → 纯前端 |

```
用户打开首页
  ├─ 从 Vuex 读取 userName、userPhone → 展示
  └─ 调用 getCarbonOverview() API
       ├─ GET /app/carbon/overview
       └─ 返回 { points } → 展示碳积分余额
```

**碳积分 API 请求链路：**

```
index.vue (created/onShow)
  → loadCarbonPoints()
    → getCarbonOverview()  [api/mine/carbon.js]
      → request.get('/app/carbon/overview')
        → 后端 CarbonController
          → 返回当前用户碳积分
```

---

#### 3. 导航网格（四个入口）

| 按钮 | 跳转页面 | 路由方式 | 实现方式 |
|------|---------|---------|---------|
| **去充电** | `/pages/charge/index` | `switchTab` | 纯前端跳转 |
| **购车** | `/pages/car/index` | `switchTab` | 纯前端跳转 |
| **碳积分** | `/pages/mine/carbon/index` | `navigateTo` | 纯前端跳转 |
| **预约维保** | `/pages/work/index` | `navigateTo` | 纯前端跳转 |

```
点击导航按钮 → handleNavClick(title)
  → 匹配 pageMap 中的 URL
  → switchTab / navigateTo 跳转
```

---

#### 4. Tab 内容区（精选科普 / 购车推荐 / 政策解读）

| Tab | 数据来源 | 实现方式 |
|-----|---------|---------|
| **精选科普** | 通过后端接口获取科普文章列表 | 有前后端交互 |
| **购车推荐** | 通过后端接口获取车型推荐列表 | 有前后端交互 |
| **政策解读** | 通过后端接口获取政策资讯列表 | 有前后端交互 |

```
switchTab(index)
  → currentTab = index
  → 调用对应后端接口获取列表数据
  → 渲染卡片列表
  → 点击卡片 → goToDetail(type, id)
    → navigateTo /pages/index/detail?type=xxx&id=xxx
```

**数据请求链路（以精选科普为例）：**

```
切换 Tab → 触发对应数据加载
  → GET /app/articles?type=science
  → 后端返回文章列表 → 渲染卡片
```

---

#### 5. 详情页（detail.vue）

| 类型 | 数据来源 | 实现方式 |
|------|---------|---------|
| 科普详情 | 通过后端接口获取文章详情 | 有前后端交互 |
| 购车详情 | 通过后端接口获取车型详情 | 有前后端交互 |
| 政策详情 | 通过后端接口获取政策详情 | 有前后端交互 |

```
detail.vue onLoad
  → 从 URL 参数获取 type 和 id
  → 调用后端接口获取详情数据
  → 渲染详情内容
```

---

## 三、前后端交互汇总

| 功能 | 接口 | 方式 |
|------|------|------|
| **用户登录** | `POST /login` | 有前后端交互 |
| **获取验证码** | `GET /captchaImage` | 有前后端交互 |
| **获取用户信息** | `GET /getInfo` | 有前后端交互 |
| **碳积分余额查询** | `GET /app/carbon/overview` | 有前后端交互 |
| **精选科普列表** | `GET /app/articles?type=science` | 有前后端交互 |
| **购车推荐列表** | `GET /app/articles?type=car` | 有前后端交互 |
| **政策解读列表** | `GET /app/articles?type=policy` | 有前后端交互 |
| **文章/车型详情** | `GET /app/articles/detail?id=xxx` | 有前后端交互 |
| Banner 数据 | 无接口（本地） | 纯前端 |
| 导航跳转 | 无接口 | 纯前端 |

---

## 四、完整执行流程

```
用户打开应用
│
├─ App.vue → checkLogin() → 无 Token 则跳转登录页
│
├─ 登录页 → 输入账号密码 → POST /login → 获取 Token
│   └─ GET /getInfo → 获取用户信息 → 存入 Vuex + Storage
│       └─ 跳转首页
│
├─ 首页 created/onShow
│   ├─ 加载碳积分 → GET /app/carbon/overview
│   ├─ 加载 Banner（本地图片）
│   ├─ 加载用户信息（Vuex）
│   └─ 加载当前 Tab 数据 → GET /app/articles?type=xxx
│
├─ 渲染页面
│   ├─ 轮播图
│   ├─ 用户卡片（用户名 + 手机号 + 碳积分）
│   ├─ 导航网格（4个图标按钮）
│   ├─ Tab 栏
│   └─ 卡片列表
│
└─ 用户交互
    ├─ 点击导航 → 跳转到对应功能页
    ├─ 点击 Tab → 切换列表（重新请求接口）
    ├─ 点击卡片 → 跳转 detail 详情页（通过接口获取详情）
    └─ 点击用户卡片 → 跳转「我的」页面
```